// Criando uma instância do ScrollReveal e configurando o reset para true
var revelar = ScrollReveal({ reset: true });

// Aplicando a animação aos elementos com a classe '.corpo3 img'
revelar.reveal('corpo3.img', {
    duration: 3000,
    distance: '90px'
});
